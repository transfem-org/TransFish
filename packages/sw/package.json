{
	"name": "sw",
	"private": true,
	"scripts": {
		"build": "pnpm swc src -d built -D",
		"watch": "pnpm swc src -d built -D -w",
		"lint": "pnpm rome check \"src/**/*.ts\""
	},
	"dependencies": {
		"calckey-js": "^0.0.20",
		"idb-keyval": "^6.2.0"
	}
}
